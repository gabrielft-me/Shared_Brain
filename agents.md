# Mandatory startup instructions

Before doing ANY task:

1. Run `git pull`
2. Read `memory/goal.md`
3. Read `memory/decisions.md`
4. Read your assigned task file
5. Never modify `goal.md` directly
6. Commit every completed task
7. Run `git pull` again before pushing
8. Resolve any conflicts before `git push`
